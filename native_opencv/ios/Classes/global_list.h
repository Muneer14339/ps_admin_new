//#ifndef GLOBAL_LIST_H
//#define GLOBAL_LIST_H
//
//#include <list>
//#include <string>
//
//// The shared container…
//extern std::list<std::string> globalList;
//
//// …and an iterator to walk through it.
//extern std::list<std::string>::iterator globalIt;
//
//// Add a new string to the back of the list.
//void addToList(const std::string& s);
//
//// Get the “next” string (advances the iterator).
//// Returns empty string when you’ve reached the end.
//std::string getNext();
//
//// Reset only the iterator back to begin().
//void resetIterator();
//
//// Completely clear the list (and reset the iterator).
//void clearList();
//
//#endif // GLOBAL_LIST_H
