// import 'package:flutter/material.dart';
// import 'package:google_fonts/google_fonts.dart';
// import 'package:pa_sreens/src/core/theme/color/app_colors_new.dart';


/* Dark Theme

final darkTheme = ThemeData.dark().copyWith(
  dialogBackgroundColor: Colors.white,
  primaryColor: Colors.grey[200],
  appBarTheme: const AppBarTheme(
    foregroundColor: Colors.grey,
    color: Colors.transparent, // Transparent app bar
    elevation: 0, // No shadow
  ),
  textTheme: ThemeData.dark().textTheme.apply(
        bodyColor: Colors.black, // Set text color to black in dark mode
        displayColor: Colors.black, // Set text color to black in dark mode
      ),
  buttonTheme: ThemeData.dark().buttonTheme.copyWith(
        buttonColor: kInitColor, // Set button color to grey in dark mode
        textTheme: ButtonTextTheme.primary,
        colorScheme: ThemeData.dark().colorScheme.copyWith(
              // Set button text color to black in dark mode
              primary: Colors.black,
            ),
      ),
  cardTheme: CardTheme(
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(
          10.0), // Set radius for the ListTile container in light mode
    ),
  ),
  listTileTheme: ListTileThemeData(
    iconColor: Colors.black,
    tileColor: Colors.grey[300], // Set ListTile color in dark mode
  ),
  inputDecorationTheme: InputDecorationTheme(
    iconColor: Colors.black,
    fillColor: Colors.grey[300],
    hintStyle: const TextStyle(color: Colors.black),
    labelStyle: const TextStyle(color: Colors.black),
    focusedBorder: const UnderlineInputBorder(
      borderSide:
          BorderSide(color: Colors.red), // Set focused border color to white
    ),
  ),
  iconTheme: const IconThemeData(color: Colors.grey),
  floatingActionButtonTheme: const FloatingActionButtonThemeData(
    backgroundColor: Colors.grey, // Set background color to grey
  ),
);
*/